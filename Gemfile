source "https://rubygems.org"

# Matches the gem set GitHub Pages builds with, so local previews
# behave the same as the deployed site.
gem "github-pages", group: :jekyll_plugins
gem "webrick"
gem "minima"
